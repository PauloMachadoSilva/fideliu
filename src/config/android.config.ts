import { MenuType } from "ionic-angular/components/menu/menu-types";

export const androidConfig = {
    scrollAssist: true,
    scrollPadding: true,
    autoFocusAssist: true,
    MenuType: 'overlay'
}